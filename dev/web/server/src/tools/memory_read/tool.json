{
  "name": "memory_read",
  "description": "Read the character's full long-term memory as a structured summary (facts/preferences/decisions), with optional keyword/topic filtering. Read-only; injected when memoryMode is read_only or editable.",
  "source": "builtin",
  "tags": ["memory"],
  "version": "1.0.0",
  "author": "TianShu",
  "dangerous": false,
  "parameters": {
    "type": "object",
    "properties": {
      "filter": { "type": "string", "description": "Keyword substring filter" },
      "topic": { "type": "string", "description": "Topic keyword grouping" },
      "limit": { "type": "number", "description": "Max entries to return" }
    },
    "required": []
  }
}
