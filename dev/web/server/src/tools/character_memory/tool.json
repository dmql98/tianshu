{
    "name": "character_memory",
    "description": "Role-private long-term memory read/write: remember (append an entry), recall (read all), forget (remove by content match). Gated by the character's memory.enabled switch and bounded by charLimit.",
    "source": "builtin",
    "tags": ["memory"],
    "version": "1.0.0",
    "author": "TianShu",
    "dangerous": false,
    "parameters": {
        "type": "object",
        "properties": {
            "action": { "type": "string", "enum": ["remember", "recall", "forget"] },
            "content": { "type": "string" }
        },
        "required": ["action"]
    }
}
